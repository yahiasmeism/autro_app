enum AuthState {
  authenticated,
  unauthenticated,
}

enum MenuItemType {
  dashboard,
  invoices,
  deals,
  proformas,
  customers,
  accounting,
  shipping,
  settings,
  bills,
  suppliers,
  messages,
}
